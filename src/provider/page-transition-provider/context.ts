import { createContext } from "react";
import { IPageTransitionProviderContext } from "../../../types";

export const PageTransitionProviderContext = createContext<IPageTransitionProviderContext>(null);
